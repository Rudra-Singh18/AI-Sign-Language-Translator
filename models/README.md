# Models

Place a trained TensorFlow Keras model here with the filename `gesture_classifier.h5`.

The application will automatically load the model from this path when it exists.

## Training

Use `train.py` after collecting gesture examples with `collect_data.py`.
